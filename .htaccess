<IfModule mod_rewrite.c>
    RewriteEngine On

    # Only redirect to /public when request is not already in /public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^$ /public/ [L]
    RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>
